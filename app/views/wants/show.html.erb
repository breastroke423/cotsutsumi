<h2>欲しいものを買う</h2>

  <%= @want.name %><br>
  <%= @want.price %><br>
  <%= @want.reason %><br>
  登録日<%= @want.created_at %><br>
        <%= button_to "購入する", {controller: 'wants', action: 'purchase'}, {method: :patch, params: {want_id: @want.id}} %>
